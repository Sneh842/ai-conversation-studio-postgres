# Copy to .env and adjust for your local/hosted Postgres instance.
DATABASE_URL=postgresql://studio_user:studio_pass@localhost:5432/ai_conversation_studio

# Optional: Gemini key can also be set here instead of via Settings page.
# GEMINI_API_KEY=
